#########################################
#
# nio makefile
#########################################

include $(CROSS_PLATFORM_LIB_DIR)/Make.include

# exe, staticlib, dynamiclib
TYPE:= exe

#output name
TARGETNAME:= server

# include  dir
INCLUDES =

# depends lib
LIBDIRS = -lboost_system-mt -lboost_thread -lbase -ltcpAsio -lpthread -lrt

#src dir
SRCDIR:= ./
 
#CCXXFLAGS= -DDEBUG -g
TOTALSRCDIR = ./tcp_server/
#OFLAGS= -L$(TARGET_PATH_R)
GLOBAL_CCXXFLAGS += -g -Wall -pthread -std=c++11 -DBOOST_SYSTEM_NO_DEPRECATED -Wl,-rpath=./ 
CCXXFLAGS = -fPIC
#CFLAGS =
#EXOBJS =
#EXCLUDESRCS =
 
#USEMYSELFALL = yes;
 
include $(CROSS_PLATFORM_LIB_DIR)/Make.share
